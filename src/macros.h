#include <iostream>
#include <fstream>
#include <sstream>
#include <optional>
#include <vector>
#include <map>
#include <any>
#include <cstdlib>

#define echo(text) std::cout << text << std::endl;
#define elif(condition) else if(condition)

